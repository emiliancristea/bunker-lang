// Test 19: Kernel Match Binding
// Purpose: Ensure identifier patterns bind the matched value inside the arm.
// Expected: `bunker run` returns 7.

kernel MatchBinding {
    fn main() -> i32 {
        let x = 3;
        let y = match x {
            v => v + 4;
        };
        return y;
    }
}
